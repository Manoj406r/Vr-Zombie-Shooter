using UnityEngine;

public class childCollisionHandler : MonoBehaviour
{
    /*public EnemyBehaviour parentEnemy;

    void Start()
    {
        // Automatically find and assign the parent enemy's script
        parentEnemy = GetComponentInParent<EnemyBehaviour>();

        if (parentEnemy == null)
        {
            Debug.LogError("EnemyBehaviour script not found on parent object!");
        }
    }

    private void OnCollisionEnter(Collision collision)
    {
        if (parentEnemy != null)
        {
            parentEnemy.HandleChildCollision(collision);
        }
    }

    private void OnTriggerEnter(Collider other)
    {
        if (parentEnemy != null)
        {
            parentEnemy.HandleChildTrigger(other);
        }
    }
    */
}
